fn main() -> int {

    x : mut int = 3;

    while (x > 100) {
        x = x - 1;
    }

    return 1;
}

fn assert(value : bool, desc : string) -> void {
    if (value == false) {
        print("Assertion failed for " + desc);
    }
}
fn print(text : string) -> void {}