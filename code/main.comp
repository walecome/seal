fn main() -> int {

    x : mut float = 0.1337 * 100.0;

    y : mut [int] = [0];

    x = 3.0;

    y[0] = 3;

    z : mut int = y[1 * 123];

    z = y[0];

    println(x);

    return 0;
}

fn assert(value : bool, desc : string) -> void {
    if (value == false) {
        println("Assertion failed for: " + desc);
    }
}
