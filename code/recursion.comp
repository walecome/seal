fn main() -> int {

    fib(20);
    recursive(10);

    return 0;
}

fn fib(n : int) -> int {
    if (n == 0) {
        return 0;
    }
    if (n == 1) {
        return 1;
    }

    return fib(n - 1) + fib(n - 2);
}

fn recursive(n : int) -> int {
    return recursive(n + 1);
}