

fn main() -> int {

    formatted := format("{} + {} = {}", 1, 2, 3);

    println(formatted);

    println("{} {}", "hello", "world");

    println("{} has is [1, 2, 3]", [1, 2, 3]);

    return 0;
}
